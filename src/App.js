import Version from './components/version/Version';

import './App.css';

function App() {
  return (
    <div className="App">
      <h1> Google HashCode mock judge system</h1>
      {/* <Button onClick={() => console.log('bot')} > click</Button> */}
      <Version />
    </div>
  );
}

export default App;
